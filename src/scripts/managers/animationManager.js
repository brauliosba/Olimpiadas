export class AnimationsManager
{
    constructor(scene){
        this.scene = scene;
    }

    load(){
        
    }

    createGameplayObjects(){
        
    }

    createAnimations(){
        /*
        if (!this.scene.anims.exists('chopsticks')){
            this.scene.anims.create({
                key: 'chopsticks',
                frames: this.scene.anims.generateFrameNumbers('chopsticks', { start: 0, end: 6, first: 0 }),
                frameRate: 15,
                repeat: 0
            });
        }
        */
    }
}   